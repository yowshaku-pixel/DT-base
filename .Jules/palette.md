## 2026-08-07 - Accessible Quotation Builders
**Learning:** For dynamic line item forms where fields are generated on-the-fly, mapping labels to inputs using unique dynamic IDs (e.g., `quote-item-desc-${item.id}`) and utilizing `sr-only` class labels for helper text when visual labels are omitted ensures a complete screen-reader and tab-navigation accessibility model.
**Action:** Ensure all dynamic lists in form modals utilize unique ID mapping combined with focus-visible ring colors matching active visual styles.
